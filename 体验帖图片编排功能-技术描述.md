# 体验帖图片编排功能 · 技术描述文档

> 版本：v1.0  
> 更新日期：2026-06-10

---

## 1. 需求概述

### 1.1 业务目标
让数码配件体验帖的作者能够按照"外观 → 接口 → 上机效果 → 桌搭全景"的叙事节奏编排图片，让浏览者能按作者安排的顺序理解配件的实际使用体验。

### 1.2 功能范围
| 角色 | 能力 |
|------|------|
| 作者（发帖） | 体验帖上传图片后，可拖拽图片到 4 个固定分组；预览完整图组节奏 |
| 浏览者（看帖） | 帖子图片按作者编排的分组顺序依次呈现，含分组标题、说明、步骤指示 |

### 1.3 约束与兼容
- 仅 **体验分享帖（type=1）** 显示编排区；问题求助帖（type=2）不显示
- **向后兼容**：旧帖（无 imageGroups 字段）保持原有平铺展示方式
- **未编排图片不丢失**：存在分组时，未放入任何分组的图片作为"其他图片"区块追加显示

---

## 2. 数据模型设计

### 2.1 数据库变更

**表：`t_post`**

| 列名 | 类型 | 说明 |
|------|------|------|
| `image_groups` | `TEXT` (新增) | JSON 数组，存储图片分组结构 |

SQL 定义（见 [schema.sql](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/resources/schema.sql#L36)）：
```sql
image_groups TEXT COMMENT '图片分组JSON数组：外观/接口/上机效果/桌搭全景'
```

**说明**：`images` 列保留原有逗号分隔 URL 列表，作为通用图片字段兼容列表页、搜索等逻辑。

### 2.2 后端数据结构

#### ImageGroupDTO / ImageGroupVO
```java
// DTO (入参) / VO (出参)
{
  "key": "appearance",       // 分组标识
  "label": "外观",            // 分组名称
  "images": ["url1","url2"], // 图片 URL 数组（有序）
  "sort": 0                  // 分组排序
}
```

文件：
- [ImageGroupDTO.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/dto/ImageGroupDTO.java)
- [ImageGroupVO.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/vo/ImageGroupVO.java)

#### 固定分组元数据（前端定义，后端不固化）
| key | label | 颜色 | 图标 | 建议用途 |
|-----|-------|------|------|----------|
| `appearance` | 外观 | `#722ed1` 紫 | 🎨 | 产品整体外观、设计语言、做工细节 |
| `interface`  | 接口 | `#13c2c2` 青 | 🔌 | 接口类型、线材、按键、连接方式 |
| `usage`      | 上机效果 | `#1890ff` 蓝 | 💻 | 装在设备上的实际效果、使用场景 |
| `desk`       | 桌搭全景 | `#fa8c16` 橙 | 🖼️ | 整体桌面环境、氛围灯光、搭配组合 |

### 2.3 TypeHandler

新增 [ImageGroupListTypeHandler.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/handler/ImageGroupListTypeHandler.java)，基于 MyBatis-Plus `AbstractJsonTypeHandler`，自动完成：
- 存储：`List<ImageGroupVO>` → JSON 字符串
- 读取：JSON 字符串 → `List<ImageGroupVO>`

Mapper 中使用：
```xml
<result column="image_groups" property="imageGroups"
        typeHandler="com.digital.community.handler.ImageGroupListTypeHandler"/>
```
见 [PostMapper.xml#L15](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/resources/mapper/PostMapper.xml#L15)

---

## 3. 后端实现

### 3.1 涉及文件清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| [Post.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/entity/Post.java#L19) | 修改 | 增加 `imageGroups` 字段 |
| [PostDTO.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/dto/PostDTO.java#L12) | 修改 | 增加 `List<ImageGroupDTO> imageGroups` |
| [PostVO.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/vo/PostVO.java#L18) | 修改 | 增加 `List<ImageGroupVO> imageGroups` |
| [PostService.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/service/PostService.java#L89-L118) | 修改 | 存储 imageGroups 并正确处理 images 字段 |
| [PostMapper.xml](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/resources/mapper/PostMapper.xml#L15) | 修改 | 查询结果映射 image_groups |

### 3.2 存储策略（PostService.create）

```
写入优先级：
1. 若请求包含 imageGroups（非空） → 写入 post.imageGroups（JSON）
2. images 字段写入规则：
   a. 如果前端传了 dto.images → 保留完整列表（含未编排图片）
   b. 如果 dto.images 为空 → 才从 imageGroups 聚合生成 images（兜底）
3. 若以上均无 → post.images 保持 null
```

**设计意图**：前端 `images` 是完整图片列表（包含已编排 + 未编排），后端以它为准。`imageGroups` 仅用于排序展示，不替代完整图片数据，避免未编排图片丢失。

代码见 [PostService.java#L96-L118](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/service/PostService.java#L96-L118)

---

## 4. 前端实现（发布端）

### 4.1 组件层级

```
CreatePost.vue
  ├── ImageUpload.vue           # 图片上传（原有，略改）
  │     └── emit('change', { all: [item, ...] })  # 每次 emit 使用 slice() 拷贝数组
  └── ImageGroupEditor.vue      # ✨ 新增：图片编排核心组件
        ├── 待编排图片区（未分组图片横排）
        ├── 4 个分组卡片（2×2 网格）
        │     └── 支持：未分组→分组拖拽、分组内排序、分组间移动
        └── 完整图组节奏预览区（垂直时间流）
```

### 4.2 核心新增组件：ImageGroupEditor

**文件**：[ImageGroupEditor.vue](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/frontend/src/components/ImageGroupEditor.vue)

**Props / Emits**：
```
Props:
  imageList: Array        // 所有已上传图片对象（含 uid/previewUrl/url/status）
  modelValue: Array       // v-model，当前编排结果 [{key,label,images:[url],sort}]

Emits:
  update:modelValue       // 编排结果变化
  change                  // 额外信息：{ groups, organizedCount, total }
```

**内部拖拽机制**（原生 HTML5 Drag & Drop API）：

| 事件 | 处理 |
|------|------|
| `dragstart` | 记录拖动源（未编排 / 某分组下某索引）+ 设置 DataTransfer |
| `dragover`  | 记录悬停位置（分组卡片 / 某图片 before / after） |
| `drop`      | 执行移动操作（同组重排序 / 跨组移动 / 未编排→分组） |
| `dragend`   | 清理所有拖动状态 |

**视觉反馈**：
- 拖动源：透明度降低 0.35 + 缩小 0.9
- 目标分组：蓝色边框 + 淡蓝背景 + 上浮 2px
- 目标图片 before/after：对应方向 3px 蓝色指示条

**编排预览区**：
当任一分组有图片时显示。采用 **垂直时间轴** 布局：
- 左侧步骤圆点（按分组配色）+ 连接线
- 右侧卡片：分组标题、说明、图片缩略图网格
- 卡片之间有向下箭头，强化"节奏"叙事感

### 4.3 CreatePost.vue 改动

**文件**：[CreatePost.vue](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/frontend/src/views/CreatePost.vue)

核心改动：
1. **图片上限提升至 20 张**（原 9 张），支持完整体验内容
2. **条件渲染编排区**：`v-if="form.type === 1 && allImageItems.length > 0"`
3. **状态进度提示**：
   - 黄色提示：还有 N 张未编排（非强制）
   - 绿色提示：全部 M 张已编排完成
4. **数据提交**：体验帖且有编排数据时，将 `imageGroups` 随请求一同发送

### 4.4 Bug 修复：数组引用问题

**Bug 1 现象**：上传多张图后，编排器只显示 1 张待编排图。

**根因**：
- [ImageUpload.vue](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/frontend/src/components/ImageUpload.vue#L589) 中 `emitChange` 的 `all` 直接传 `imageList.value`（同一个数组引用）
- Vue props 浅比较检测不到引用未变的数组内容变化
- 下游 computed 不触发重算

**修复**（双保险）：
```js
// ImageUpload.vue emitChange
all: imageList.value.slice()   // 每次 emit 浅拷贝

// CreatePost.vue allImageItems
const allImageItems = computed(() => {
  const list = imageInfo.value.all || []
  return list.slice()           // 下游再拷贝一次
})
```

### 4.5 分组与上传区同步

为避免作者先完成编排、后又回到上传区删除图片时分组中残留脏数据，[ImageGroupEditor.vue](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/frontend/src/components/ImageGroupEditor.vue) 增加了 `allImages` 监听：

- 当前上传区图片 UID / URL 作为有效集合
- 分组内图片一旦不再存在于上传区，就自动从对应分组移除
- `organizedCount`、预览区、提交数据会同步收敛，不会把已删除图片提交出去

---

## 5. 前端实现（浏览端）

### 5.1 PostDetail.vue 渲染策略

**文件**：[PostDetail.vue](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/frontend/src/views/PostDetail.vue)

```
进入帖子 → 检测 post.imageGroups 是否有效：
  ├── 有有效分组 → 走"分组展示"模式
  │     ├── 顶部引导条：作者按以下节奏分享使用体验
  │     ├── 按 sort + key 排序分组（外观→接口→上机→桌搭）
  │     ├── 每个分组：步骤圆点(彩色) + 连接线 + 卡片(标题/说明/图片网格)
  │     └── ➕ 尾部：未编排的其他图片（灰色圆点+卡片）
  └── 无分组 → 走"平铺展示"模式（兼容旧帖）
```

### 5.2 图片排序逻辑

```js
// sortedImageGroups 计算属性
const sortOrder = ['appearance', 'interface', 'usage', 'desk']
排序优先级：
  1. group.sort 字段（数值越小越前）
  2. sortOrder 中的固定位置
  3. key 字母序（兜底）
```

### 5.3 未编排图片的处理

```js
// 计算分组内 URL 集合
const groupedImageSet = new Set()
sortedImageGroups.forEach(g => g.images.forEach(url => set.add(url)))

// 从 post.images 全集里剔除已编排 URL = 未编排图片
const ungroupedImages = post.images.filter(url => !groupedImageSet.has(url))
```

渲染为灰色调的"🖼️ 其他图片"分组，排在四色分组之后。

### 5.4 图片预览器

**统一预览对话框**（兼容分组 / 平铺 / 未编排 三种模式）：

- **快捷键**：← / → 切换，Esc 关闭，滚轮缩放
- **标题信息**：
  - 分组图片：🎨 外观 · 第 2 张
  - 未编排图片：🖼️ 其他图片 · 第 1 张
  - 旧帖平铺：仅显示 `3 / 12`（当前/总数）
- **缩放范围**：0.5x ~ 3x
- **序号修正**：未编排图片在预览器中按“其他图片”组内序号计数，不再沿用全局平铺序号

### 5.5 Bug 修复：未编排图片被吞掉

**Bug 2 现象**：存在 imageGroups 时，未编排图片既不出现在存储中，也不出现在详情渲染中。

**根因链路**：
```
存储：PostService.create 先用 dto.images 写 post.images
              └─ 后用分组聚合 allGroupImages 覆盖 post.images → 未编排丢失
渲染：PostDetail 中 v-if="hasImageGroups" ... v-else-if="post.images"
              └─ 两个分支互斥，有分组时不渲染 images → 未编排丢失
```

**修复**：
1. **后端**：见 §3.2，仅在 dto.images 为空时才从分组聚合
2. **前端**：分组展示模式下追加"其他图片"区块 + `flatPreviewList` 合并未编排图片

---

## 6. 完整数据流

```
作者操作：
  上传图片 → ImageUpload 内部 imageList (ref)
           → emitChange { all: [...copy], images: [urls] }
           → CreatePost imageInfo.value.all + images.value
           → ImageGroupEditor props.imageList (响应式更新)
           → 拖拽分组 → ImageGroupEditor groups.value 变化
           → emit update:modelValue [{key,label,images:[url],sort}]
           → CreatePost imageGroups.value
           → 提交 createPost({ ..., images, imageGroups })

后端处理：
  PostDTO { images, imageGroups }
    → PostService.create()
    → post.imageGroups = JSON.stringify(imageGroups)
    → post.images     = String.join( ",", images || 聚合自分组 )
    → INSERT t_post

浏览者访问：
  GET /posts/:id
    → PostMapper → PostVO { images (List<String>), imageGroups (List<ImageGroupVO>) }
    → PostDetail.vue
    → hasImageGroups ? 分组展示 : 平铺展示
    → 点击图片 → 统一预览器
```

---

## 7. 构建与验证

| 环境 | 命令 / 动作 | 结果 |
|------|-------------|------|
| 前端 | `npm run build` | ✅ exit code 0 |
| 后端 | `docker compose up -d --build backend frontend` | ✅ 后端镜像重新编译并启动成功 |
| 页面可达性 | `curl -I http://127.0.0.1:3009/` / `curl http://127.0.0.1:8089/api/categories` | ✅ 前后端可访问 |
| 发布页实测 | 临时 Playwright 脚本上传 4 张 GIF | ✅ 编排器显示 `已编排 0 / 4`，待编排图数量为 4 |
| 详情页实测 | 真实创建含 4 个分组 + 1 张未编排图的帖子 | ✅ `images` 返回 5 张，页面展示 5 个图片区块（含“其他图片”） |

### 7.1 本次实测结果

本次浏览器与接口联动验证得到的关键结果如下：

```json
{
  "createCheck": {
    "unassignedCount": 4,
    "organizedText": "已编排 0 / 4",
    "uploadText": "4 / 20 张"
  },
  "apiCheck": {
    "imageCount": 5,
    "imageGroupsCount": 4,
    "containsUngrouped": true
  },
  "detailCheck": {
    "introText": "作者按以下节奏分享使用体验",
    "groupedBlockCount": 5,
    "ungroupedTitle": "其他图片",
    "ungroupedCountText": "1 张图"
  }
}
```

---

## 8. 文件索引（变更总览）

### 新增文件
- [ImageGroupDTO.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/dto/ImageGroupDTO.java)
- [ImageGroupVO.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/vo/ImageGroupVO.java)
- [ImageGroupListTypeHandler.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/handler/ImageGroupListTypeHandler.java)
- [ImageGroupEditor.vue](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/frontend/src/components/ImageGroupEditor.vue)
- [体验帖图片编排功能-技术描述.md](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/体验帖图片编排功能-技术描述.md)

### 修改文件
- [schema.sql](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/resources/schema.sql#L36)
- [Post.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/entity/Post.java#L19)
- [PostDTO.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/dto/PostDTO.java#L12)
- [PostVO.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/vo/PostVO.java#L18)
- [PostService.java](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/java/com/digital/community/service/PostService.java#L89-L118)
- [PostMapper.xml](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/backend/src/main/resources/mapper/PostMapper.xml#L15)
- [ImageUpload.vue](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/frontend/src/components/ImageUpload.vue#L589)（all 浅拷贝修复）
- [CreatePost.vue](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/frontend/src/views/CreatePost.vue)（编排区整合）
- [PostDetail.vue](file:///Users/Admin/Desktop/solo-0601/qd-0601/qd-109/frontend/src/views/PostDetail.vue)（分组展示 + 预览器）
