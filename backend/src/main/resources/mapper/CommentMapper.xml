<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.digital.community.mapper.CommentMapper">

    <resultMap id="CommentVOMap" type="com.digital.community.vo.CommentVO">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="user_id" property="userId"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="parent_id" property="parentId"/>
        <result column="reply_user_id" property="replyUserId"/>
        <result column="reply_nickname" property="replyNickname"/>
        <result column="content" property="content"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectCommentsByPostId" resultMap="CommentVOMap">
        SELECT c.*, u.nickname, u.avatar, ru.nickname as reply_nickname
        FROM t_comment c
        LEFT JOIN t_user u ON c.user_id = u.id
        LEFT JOIN t_user ru ON c.reply_user_id = ru.id
        WHERE c.post_id = #{postId} AND c.deleted = 0
        ORDER BY c.create_time ASC
    </select>

</mapper>
